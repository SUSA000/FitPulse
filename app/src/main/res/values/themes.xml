<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WellnessTrack" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textColorHint">@color/textColorHintDark</item>
        <item name="colorPrimary">@color/primary_orange</item>
        <item name="colorOnPrimary">@color/text_primary</item>
        <item name="colorSurface">@color/primary_orange</item>
        <item name="android:windowBackground">@color/bg_warm</item>
    </style>

    <style name="Theme.WellnessTrack" parent="Base.Theme.WellnessTrack" />
</resources>
